# framenote

렌더된 영상에 **프레임 단위로 메모**를 남기고, 그 메모를 코딩 에이전트가 바로 받는다.

```bash
npx framenote out/final.mp4
```

브라우저가 열린다. 영상을 보다가 화면을 끌면 그 자리의 프레임·좌표가 메모에 자동으로 붙는다.
"에이전트에게 보내기"를 누르면 감시 중인 에이전트가 알림을 받고 고친다.

> 설계와 요구사항은 라쏘런 문서가 원본이다. 구현 전이므로 이 README 는 자리만 잡아 둔다.
