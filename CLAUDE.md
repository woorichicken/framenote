# framenote

영상 리뷰 메모를 코딩 에이전트가 실행 가능한 지시로 바꾸는 로컬 도구.

## 이 저장소가 지키는 것

- **의존 없음**: feedback-kit 을 import 하지 않는다. 코드는 참고만 한다
  (`~/Downloads/feedback-kit`). 서버 전송이 없으니 큐·재시도·진단이 필요 없다.
- **파일이 원본**: 메모의 단일 원천은 `notes.jsonl` 이다. 서버는 문지기일 뿐이라
  죽어도 메모가 남고, 에이전트는 서버 없이 파일만 읽어도 일할 수 있다.
- **이미지는 파일로**: 첨부는 파일로 저장하고 메모에는 경로만 적는다.
  base64 를 메모에 박으면 에이전트 컨텍스트가 터진다.
- **밖으로 안 나간다**: 수집 서버·트리아지 연동을 넣지 않는다. 로컬에서 끝난다.

## 아직 없는 것

전부. 문서와 TC 를 먼저 쓰고 구현한다.

## 명령

```bash
pnpm all        # typecheck + test + build. 커밋 전 이것 하나만 통과시키면 된다
pnpm typecheck
pnpm test
pnpm build
pnpm ruler      # 프레임 눈금 영상 생성 (프레임 정확도 TC 의 전제)
```

`pnpm ruler` 는 프레임마다 그 번호를 태운 영상을 만든다. **프레임 정확도 TC 5건이 이 영상 없이는
못 돈다.** ffmpeg 의 `drawtext` 를 쓰지 않는다 — 이 맥의 ffmpeg 에는 그 필터가 없고(freetype
없이 빌드됨), 검증 도구가 환경에 따라 안 돌면 검증을 안 하게 된다. 숫자를 픽셀로 직접 그려
rawvideo 로 파이프하므로 어느 ffmpeg 빌드에서도 돈다.

## 계약이 사는 곳

`src/types.ts` 가 저장 형식과 에이전트 접점의 정본이다. 요구사항 원본은 라쏘런 문서이고,
둘이 어긋나면 **문서가 맞다.** 고칠 때 양쪽을 같이 옮긴다.

## 외부 의존

`ffprobe`(영상 규격 읽기)와 `ffmpeg`(눈금 영상 생성)가 시스템에 있어야 한다. npm 의존성은 0 이다.
