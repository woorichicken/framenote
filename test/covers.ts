/**
 * 이 테스트가 어떤 요구사항 테스트케이스를 덮는지 선언한다.
 *
 * 테스트 제목이 TC 제목과 같으면 이걸 쓸 필요가 없다. 한 테스트가 **여러** TC 를 덮거나
 * 제목을 다르게 쓸 수밖에 없을 때만 쓴다. `pnpm tc:check` 가 이 선언을 읽어
 * "짝이 없는 TC" 를 잡는다.
 *
 * 실행 시점에는 아무 일도 하지 않는다 — 검사기가 소스에서 읽는다.
 */
export function covers(..._titles: string[]): void {
  /* 표식일 뿐이다 */
}
